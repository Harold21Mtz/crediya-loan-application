server:
  port: 8091
spring:
  application:
    name: "crediya-application"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8091"
adapters:
  r2dbc:
    host: localhost
    port: 5432
    database: crediya-loan-application
    schema: public
    username: postgres
    password: Harold123
logging:
  level:
    root: INFO
    com.application: TRACE
